using UnityEngine;

public class Davethemagicalcheesewizard : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("I am Dave the Magical Cheese Wizard");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
